{
  "name": "@meetingos/contracts",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"TODO: assignee expands contracts linting\"",
    "test": "vitest run src"
  },
  "dependencies": {
    "zod": "^3.25.32"
  }
}
