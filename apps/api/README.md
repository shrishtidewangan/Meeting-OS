# API Starter

This Express app starts and exposes `GET /health`.

All MeetingOS product routes, controllers, services, repositories, models, graph nodes, and model clients are placeholders for assignee implementation.

