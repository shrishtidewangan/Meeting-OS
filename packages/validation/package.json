{
  "name": "@meetingos/validation",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --config ../../eslint.config.js",
    "test": "vitest run src"
  },
  "dependencies": {
    "@meetingos/contracts": "workspace:^",
    "zod": "^3.25.32"
  },
  "devDependencies": {
    "vitest": "^3"
  }
}
